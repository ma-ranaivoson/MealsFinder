export interface Fonts {
  fonts: {
    body: string;
    heading: string;
    monospace: string;
  };
  fontWeights: {
    regular: number,
    medium: number,
    bold: number,
  };
  fontSizes: {
    caption: string,
    button: string,
    body: string,
    title: string,
    h5: string,
    h4: string,
    h3: string,
    h2: string,
    h1: string,
  }
}
